aws is a cloud platform which provides cloud infrastructure for servers, database, networking, storage
instead of maintaining physical servers company user aws services
apy as you go means we have to pay as much we use 
help to cost saving
easy to scale if required
securre 
and reach gloabaly with diff timezone

EC2 for run application on server provide virtual server
lambda where we can run code 
s3 for simple storage service file media , backup we can store
RDS relational  database service to where we can run our database
aws iam for user security and role management
cloudwatch for monitering 
api gateway for gateway